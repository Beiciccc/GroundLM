# Pinned to the versions used to produce the reported numbers.
# --- analysis (CPU; reproduces every paper number from cached features) ---
numpy==2.1.3
scipy==1.15.3
scikit-learn==1.8.0
matplotlib==3.10.0
pyyaml>=6.0
tqdm>=4.66
# --- extraction (GPU node; regenerates runs/*/features.npz from scratch) ---
# the GPU run used torch 2.11 / transformers 5.12 / datasets 5.0; these lower
# bounds are compatible with the cached-feature analysis above.
torch>=2.10
transformers>=5.5
datasets>=5.0
accelerate>=0.30
safetensors>=0.4
sentencepiece>=0.2
protobuf>=4.0
